---
title: Java Base
author: ShengJun
date: 2024-05-07 10:00:00 +0800
categories: [Java, Resume]
tags: [Java]
render_with_liquid: false
---

## Java基础

- 0. Java
```
Java是一门面向对象的编程语言，吸收了C++语言的优点，静态面向对象编程语言的代表

Java SE：标准版，面向部署在桌面、嵌入式环节

Java EE：企业版，在SE基础上构建，可部署移植性更强

Java ME：微型版，在移动设备和嵌入式设备上运行
``` 

- 1. JVM、JRE、JDK
```
JVM: Java Virtual Machine是Java虚拟机，Java程序需要运行在虚拟机上，
	 不同的平台有自己的虚拟机，因此Java语言可以实现跨平台。

JRE: Java Runtime Environment包括Java虚拟机和Java程序所需的核心类库等。 
	 核心类库主要是java.lang包：包含了运行Java程序必不可少的系统类，
	 如基本数据类型、基本数学函数、字符串处理、线程、异常处理类等，系统缺省加载这个包。

JDK: Java Development Kit是提供给Java开发人员使用的，
	 其中包含了Java的开发工具，也包括了JRE。所以安装了JDK，
	 就无需再单独安装JRE了。其中的开发工具：编译工具(javac.exe)，打包工具(jar.exe)等

Android 5.0以前JVM是Dalvik	 
Android 5.0开始JVM是ART(Android Runtime)，提高执行效率，减少卡顿	 
```

- 2. 字节码
```
字节码: Java源代码经过虚拟机编译器编译后产生的文件（即扩展为.class的文件），
	   它不面向任何特定的处理器，只面向虚拟机。
	   
优点 ： Java语言通过字节码的方式，在一定程度上解决了传统解释型语言执行效率低的问题，
	   同时又保留了解释型语言可移植的特点。所以Java程序运行时比较高效，
	   而且由于字节码并不专对一种特定的机器，
	   因此Java程序无须重新编译便可在多种不同的计算机上运行。 

过程 ： Java源代码 -> 编译器 -> JVM可执行的Java字节码(即虚拟指令) -> JVM -> JVM中解释器 -> 机器可执行的二进制机器码 -> 程序运行
```

- 3. Java语言特点
```
(C语言面向过程： 优点：性能高，没有类调用的实例化、开销小； 缺点： 维护难、复用低、扩展低)

面向对象：优点：易维护、易复用、易扩展，由于面向对象有封装、继承、多态性的特性，可以设计出低耦合的系统，使系统更加灵活、更加易于维护
		缺点：性能比面向过程低
		
(面向对象的三大特性： 封装性、 继承性、 多态性)

封装性： 隐藏对象的属性和实现细节，仅对外提供公共访问方式，将变化隔离，便于使用，提高复用性和安全性。

继承性： 子类继承父类的特征和行为 (子类拥有父类非 private 的属性和方法、可拥有自己的成员与方法)

多态性： 父类或接口定义的引用变量可以指向子类或具体实现类的实例对象。提高了程序的拓展性。
        (多态有三个必要条件：继承、重写、向上转型)
```

- 4. 访问修饰符： public、protected、private、不使用
```
public: 对所有类可见
private: 对同一类可见
protected: 对同一包内的类和所有子类可见
不使用修饰符： 在同一包内可见，不使用任何修饰符
```

- 11. Java类加载机制
```
类加载机制: 将.class文件中的二进制数据读入到内存中，并对数据进行校验，解析和初始化;

类加载过程： 加载 -> 验证 -> 准备 -> 解析 -> 初始化
```

- 11. Java 反射
```
特点： 为开发提供了极大的灵活性，允许修改类成员访问属性，但执行效率低

Class cls = Class.forName("类名");
```

- 4. 抽象类与接口
```
1、都不能实例化
2、抽象类只能被继承
3、接口只能被实现
```

- 5. 内部类：成员内部类、局部内部类、匿名内部类和静态内部类
```
成员内部类: 定义在类内部，成员位置上的非静态类

局部内部类: 定义在方法中的内部类

匿名内部类: 通常用于创建实现某个接口或继承某个类的子类的对象。
		  它没有类名，直接继承或实现某个类或接口，并且可以在同一行中创建对象。
		  (匿名内部类必须继承一个抽象类或者实现一个接口，不能定义任何静态成员和静态方法， eg：Runnable、ClickListener)
		  
静态内部类: 定义在类内部的静态类
```

- 6. 重载、重写
```
方法的重载和重写都是实现多态的方式，区别在于前者实现的是编译时的多态性，而后者实现的是运行时的多态性。

重载：发生在同一个类中，方法名相同参数列表不同（参数类型不同、个数不同、顺序不同），
     与方法返回值和访问修饰符无关，即重载的方法不能根据返回类型进行区分

重写：发生在父子类中，方法名、参数列表必须相同，返回值小于等于父类，抛出的异常小于等于父类，
     访问修饰符大于等于父类（里氏代换原则）；如果父类方法访问修饰符为private则子类中就不是重写。
```

- 7. 堆、栈、方法区
```
堆： 堆是Java内存管理中用于存储Java对象实例。与C++不同，Java中的内存分配和释放完全由虚拟机（JVM）自动管理，
	(重要特点：其大小在程序运行时是可以动态调整的。JVM会根据程序的需要自动调整堆内存的大小)
	(所有new出来的对象成员变量)

栈： 栈是Java内存中用于存储基本数据类型、对象引用以及方法调用的上下文信息。 遵循“后进先出”（LIFO）的原则
	(基本数据类型、对象引用、局部变量存放在栈中)

方法区： xxx.class、 static变量、 常量池


常量池： String a = "str"; 存放在常量池中(高效且省内存)
        String a = new String("str"); 存放在堆中
		基本数据常量(public static final)		

static： 修饰成员变量、方法
         修饰代码块：类加载的时候只执行一次(内存分配)，eg： 加载so库
		 (被static修饰，不属于任何实例的对象，属于被类的实体对象所共享)
		 (静态只能访问静态， 非静态可以访问静态)
```

- 8 Java数据类型
```
数据类型： 引用数据类型： 类class、 接口interface、 数组[]
		 基本数据类型：布尔型boolean、字符型char、 数值型(整数类型、浮点类型)
```

- 8. 基本数据类型
```
整型：  byte   1字节
	   short  2字节
	   int    4字节
	   long   8字节

浮点型： float  4字节 (赋值末尾： f, 单精度, 否则默认double)
	    double 8字节 (赋值末尾： d, 双精度, 否则默认double)

字符型： char 2字节	

布尔型： boolean 1字节 (true 、false)

(基本数据的封装类型：Byte、 Short、 Integer、 Long、 Float、 Double、 Character、 Boolean)	
```

- 9. == 和 equals 的区别
```
== : 用于基本数据类型时，判断值是否相同；
     用于引用类型时，判断内存地址是否相同；(除同一个new出来的对象结果为true，其它都为false)

equals：默认情况下(在不重写类的equals()情况下)，对比内存地址是否相同
	(所有的类都继承自 Object， 其中有equals())
```

- 10. Java IO
```
按照方向： 输入流： InputStream/Reader
		 输出流： OutputStream/Writer

按照类型： 字节流： InputStream/OutputStream
		 字符流： Reader/Writer
		 
BIO: Block IO 同步阻塞式 IO
NIO: Non IO   同步非阻塞 IO

File.exists()：检测文件路径是否存在。
File.createFile()：创建文件。
File.createDirectory()：创建文件夹。
File.delete()：删除一个文件或目录。
File.copy()：复制文件。
File.move()：移动文件。
File.size()：查看文件个数。
File.read()：读取文件。
File.write()：写入文件
```

- 12. String、StringBuffer、StringBuilder
```
String: 线程安全, 不可变(字符串相加时组成了一个新的String常量)
StringBuffer: 线程安全
StringBuilder: 线程不安全

(都是被final修饰，不可被继承)
效率： StringBuilder > StringBuffer > String
```

- 13. final finally finalize
```
final: 修饰成员属性是否可修改、继承、重写
finally: 使用在try、catch代码块中，始终(包含return)会进入finally
finalize: Object类的方法，由垃圾回收器来调用，System.gc()主动触发垃圾回收
```

- 14. 多线程
```
Handler， HandlerThread
Looper，MessageQueue，Handler，Message
ReentrantLock、Synchronized、Volatile 
线程阻塞的方式。主要还是考察sleep、wait、yield与join的区别 
ThreadLocal

join()
wait() 
nofiy()
synchronized
volitate

```

- 15. 数据结构
```
Set、Table、Vector、Link、 List

HashMap、Hashtable、LinkedHashMap、ConcurrentHashMap、ArrayList、LinkedList
```

- 16. LruCache、 DiskLruCache
```
1、三级缓存
2、手写图片加载
```

- 17. 设计模式
```
单例模式
适配器模式
建造者模式
代理模式
```