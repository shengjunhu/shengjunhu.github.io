### Android基础

- 1. Activity
```
1、生命周期

2、启动模式:
	standard
	singleTask
	singleTop
    singleInstance
	
3、onSaveInstanceState(Bundle outState)

4、onNewIntent()
```

- 2. Service
```
startService()

bindService()
```

- 3. Broadcast Receiver


- 4. Content Provider


- 5. Context


- 5. Intent
```
a、IntentService IntentFilter
	b、host
	c、path
	d、action
```

- 5. Fragment
```
1、生命周期：
	onAttach(): 当Fagment和Activity产生关联时被调用；
	onCreate(): 当Fragment被创建时调用；
	onCreateView(): 创建并返回与Fragment相关的view界面；
	onViewCreate(): 在onCreateView执行完后立即执行；
	onActivityCreated(): 通知Fragment，他所关联的Activity已经完成了onCreate的调用；
	onStart(): 让Fragment准备可以被用户所见，该方法和Activity的onStart（）方法相关联；
	onResume(): Fragment可见，可以和用户交互，该方法和Activity的onResume方法相关联；
	onPause(): 当用户离开Fragment时调用该方法，此操作是由于所在的Activity被遮挡或者是在Activity中的另一个Fragment操作所引起的；
	onStop(): 对用户而言，Fragment不可见时调用该方法，此操作是由于他所在的Activity不再可见或者是在Activity中的一个Fragment操作所引起的；
	onDestroyView(): ment清理和它的view相关的资源；
	onDestroy(): 最终清理Fragment的状态；
	onDetach(): Fragment与Activity不再产生关联；

2、Fragment与Activity

3、Fragment与Fragment

4、FragmentPagerAdapter与FragmentStatePagerAdapter

5、getFragmentManager、getSupportFragmentManager、getChildFragmentManager
```

- 5. RecyclerView

- 6. 自定义控件


- 7. 触摸事件分发


- 8. App启动流程


- 9. Android OS Diff
```
Android 14		U				34
Android 13		T				33
Android 12L		S				32
Android 12.0	S				31
Android 11.0	R				30
Android 10.0	Q				29 	requestLegacyExternalStorage = true
Android 9.0		Pie				28
Android 8.1		Orea			27
Android 8.0		Orea			26
Android 7.1.1	Nougat			25
Android 7.0		Nougat			24	存储、分屏
Android 6.0		MarshMallow		23	运行权限
Android 5.1		Lollipop		22
Android 5.0		Lollipop		21
Android 4.4W 	KitKat Wera		20
Android 4.4		KitKat			19
```

- 4. Handler、HandlerThread


- 10. Handler、Message Queue、Looper


- 10. Parcelable和Serializable


- 10. Storage方式


- 11. 布局、屏幕适配


- 12. 多模块初始化


- 13. 开发模式：MVC、MVP、MVVM


- 14. AIDL、多进程、IPC、Binder


- 16. 插件化
```
Boundle

360plugin

Tinker
```

- 17. JNI、NDK
```
AttachCurrentThread 
DetachCurrentThread 
JNIEnv 表示 Java 调用 native 语言的环境, 不能跨线程
```

- 18. Android Camera1
```

```

- 19. Android Camera2
```

```

- 20. Third Library
```
OKHttp、Retrofit

Glide
```

- 21. 内存泄漏与内存溢出
```
1、内存泄漏：

2、内存溢出：

3、在多数设备上，默认情况下，每个应用程序的最大内存限制通常在 32MB 到 48MB 之间；
     android:largeHeap="true"	
```

- 22. AS工具调优


### 优化选项
- 启动优化
- UI渲染优化
- 崩溃优化
- 内存优化
- 卡顿优化
- 存储优化
- 网络优化
- 耗电优化
- 多线程并发优化
- 安装包优化

### APP更新
- dex更新

- bundle更新

### Jetpack
- Lifecycle
- ViewModel
- LiveData
- Navigation
- Room
- WorkManager
- Pagging
- Databinding
- Datastore

### Framework
- Handler

- Binder

- zygote

- AMS

- PMS

- WMS

- Launcher

- Camera2

- SELinux


### Android Source
```
1、Framework

2、AMS

3、WMS

4、Launcher

5、SELinux
```